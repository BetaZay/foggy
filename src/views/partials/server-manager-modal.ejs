<div x-cloak x-show="serverManagerOpen" class="fixed inset-0 z-[70] flex items-center justify-center p-4 sm:p-6" role="dialog" aria-modal="true" aria-label="FOG server manager">
  <div class="absolute inset-0 bg-stone-950/55 backdrop-blur-[2px]" x-transition.opacity @click="serverManagerOpen = false"></div>
  <div x-show="serverManagerOpen" x-transition class="relative z-10 w-full max-w-2xl overflow-hidden rounded-2xl border border-white/30 bg-white shadow-2xl">
    <div id="server-manager-content" class="min-h-48" aria-live="polite"><div class="grid min-h-48 place-items-center"><div class="flex items-center gap-3 text-sm text-stone-500"><span class="size-4 animate-spin rounded-full border-2 border-stone-300 border-t-fog-700"></span>Loading connections…</div></div></div>
  </div>
</div>
